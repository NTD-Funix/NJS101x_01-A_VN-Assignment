<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/index.css">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="/css/forms.css">
</head>

<body>
   <%- include('../includes/navigation.ejs') %>

    <main>
        <div class="grid">
            <article class="card index-item ">
                <header class="card__header">
                    <h1 class="index__title">Checkout Info</h1>
                </header>
                <hr>
                <div class="container">
                    <% if(todayWorkingInfo) { %>
                    <h4>Số giờ đã làm hôm nay: <%= todayWorkingInfo.totalTimeOfDay %> giờ</h4>
                    <div class="mt-3">
                        <table class="table table-bordered">
                            <thead>
                                <tr class="table-success text-center">
                                    <th>STT</th>
                                    <th>Thời gian bắt đầu</th>
                                    <th>Thời gian kết thúc</th>
                                    <th>Nơi làm việc</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% todayWorkingInfo.detailsTime.forEach(item => { %>
                                <tr class="text-center">
                                    <td><%= todayWorkingInfo.detailsTime.indexOf(item) + 1 %></td>
                                    <td><%= item.startTime.toISOString() %></td>
                                    <td><%= item.endTime.toISOString() %></td>
                                    <td><%= item.workPlace %></td>
                                </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                    <h4>Trạng thái: <%= employee.status === 1 ? 'Đang làm việc' : 'Không làm việc'%></h4>
                    <hr>
                    <div class="row">
                        <div class="col-sm-6 mb-3 text-center">
                            <a href="/checkin-form" class="btn btn-success <%= employee.status === 1 ? 'disabled' : ''; %>">Điểm danh</a>
                        </div>
                        <div class="col-sm-6 mb-3 text-center">
                            <a href="/" class="btn btn-primary">Trang chủ</a>
                        </div>
                    </div>
                    <% } else { %>
                        <h4>Chưa có dữ liệu!</h4>
                    <% } %>
                </div>
            </article>
        </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<%- include('../includes/end.ejs') %>


